services:
  backend:
    build: .
    command: npm run dev
    ports:
      - "5000:3000"
    environment:
      - PORT=3000
      - REDIS_URL=redis://redis:6379
      - MILVUS_URL=milvus:19530
      - JWT_SECRET=changeme
      - POSTGRES_HOST=db # Use the service name 'db' as the hostname for the database
      - POSTGRES_USER=admin
      - POSTGRES_PASSWORD=admin123
      - POSTGRES_DB=postgres_db
      - POSTGRES_PORT=5432
      - JWT_SECRET=your_super_secret_key
    depends_on:
      - db
      # - redis
      # - milvus
    volumes:
      - .:/app
    working_dir: /app
  db:
    image: postgres:15
    restart: always
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    ports:
      - "${POSTGRES_PORT}:5432"
    volumes:
      - ./initdb:/docker-entrypoint-initdb.d
      - postgres-data:/var/lib/postgresql/data
  # redis:
    # image: redis:7
    # restart: always
    # ports:
    #   - "6379:6379"
  # milvus:
    # image: milvusdb/milvus:v2.3.9
    # restart: always
    # ports:
    #   - "19530:19530"
    #   - "9091:9091"
    # This is a placeholder config. Adjust as needed for real Milvus usage.


volumes:
  postgres-data: